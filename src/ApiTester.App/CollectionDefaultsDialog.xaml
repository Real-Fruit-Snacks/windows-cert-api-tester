<Window x:Class="ApiTester.App.CollectionDefaultsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Set website &amp; certificate" SizeToContent="Height" Width="560"
        WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        Background="{StaticResource Bg.Window}" Foreground="{StaticResource Text.Soft}"
        FontFamily="{StaticResource Mono}" FontSize="12.5"
        TextOptions.TextFormattingMode="Ideal" UseLayoutRounding="True">
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" ResizeBorderThickness="0" CornerRadius="0"
                      GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>
    <Border BorderBrush="{StaticResource Border.Strong}" BorderThickness="1" Padding="18">
        <StackPanel>
            <TextBlock x:Name="HeaderText" FontSize="15" FontWeight="SemiBold"
                       Foreground="{StaticResource Text.Normal}" Margin="0,0,0,4"/>
            <TextBlock Text="Requests opened from this collection use these when they don't have their own."
                       FontSize="12" Foreground="{StaticResource Text.Muted}" TextWrapping="Wrap" Margin="0,0,0,14"/>

            <TextBlock Text="WEBSITE" Style="{StaticResource Caption}" Margin="0,0,0,4"/>
            <DockPanel Margin="0,0,0,12">
                <ComboBox x:Name="SavedCombo" Width="34" DockPanel.Dock="Right" Margin="6,0,0,0"
                          SelectionChanged="SavedCombo_SelectionChanged"/>
                <TextBox x:Name="BaseUrlBox" VerticalContentAlignment="Center"/>
            </DockPanel>

            <TextBlock Text="CERTIFICATE" Style="{StaticResource Caption}" Margin="0,0,0,4"/>
            <ComboBox x:Name="CertCombo" Margin="0,0,0,18"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Clear defaults" Width="110" Margin="0,0,8,0" Click="Clear_Click"/>
                <Button Content="Cancel" Width="90" Margin="0,0,8,0" IsCancel="True"/>
                <Button Content="Save" Width="90" Style="{StaticResource PrimaryButton}" IsDefault="True" Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </Border>
</Window>
